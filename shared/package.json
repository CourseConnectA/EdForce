{
  "name": "@edforce/shared",
  "version": "1.0.0",
  "description": "Shared types and interfaces for the Edforce system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  },
  "files": [
    "dist"
  ]
}